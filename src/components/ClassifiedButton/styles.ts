import styled from 'styled-components';

export const Container = styled.div`
  > button {
    width: 175px;
    height: 48px;
    background: var(--low-blue);
    color: var(--blue);
    font-size: 1rem;
    font-weight: 500;

    border: 0;
    border-radius: 10px;
  }
`;
